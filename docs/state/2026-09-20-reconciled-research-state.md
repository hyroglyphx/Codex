# Reconciled Research State — 2026-09-20

This checkpoint bridges the immediately preceding research chat into the GitHub-backed Codex while preserving provenance.

## Status vocabulary

- **Recovered** — explicitly preserved from prior work.
- **Reconstructed** — assembled from multiple preserved fragments; structurally justified but not claimed as verbatim prior state.
- **Generated** — new work created at this checkpoint.
- **Missing** — referenced prior material whose exact enumeration/text is not preserved.

## Recovered

### Platform architecture

```text
Confluence → Relay Kernel → Nexus → Thought Sieve/Council → Adversary → Organon → Builder
```

Relay outputs return as ResultPackets to accepted state or an exception path.

### Canonical OrganonEvent fields

`event_id, timestamp, epoch, actor, authority, object_id, object_type, domain, event_type, parent_events[], operation, representation, input_state, output_state, preserved[], altered[], lost[], introduced[], evidence[], counterevidence[], confidence, maturity, dependencies[], conflicts[], return_path, notes`

Events are immutable. Corrections append challenge/test/supersede/replacement events.

### Authority

L0 Source → L1 User → L2 Canonical → L3 Validated → L4 Hypothesis.

### Promotion

`OBSERVED → CANDIDATE → TESTED → CORROBORATED → CANONICAL`

Permanent/non-promoting states: `FAILED`, `CONTRADICTED`, `SUPERSEDED`, `Ω-UNRESOLVED`.

### IER object grammar

```text
X = (O, P, R, A, I, T, C, G)
```

- O — Object
- P — Partition
- R — Representation
- A — Admissible Operation
- I — Invariant
- T — Attractor
- C — Compression
- G — Regeneration

Canonical verbal form:

```text
Object → Partition → Representation → Admissible Operations
→ Invariant Extraction → Attractor Detection → Compression → Regeneration
```

### Δ-001 benchmark skeleton

Ten passes:

1. recover objects,
2. recover claims,
3. recover derivations,
4. recover mutations,
5. recover rediscoveries,
6. recover contradictions,
7. IER-normalize,
8. Council ablation,
9. temporal holdout,
10. Organon selects the next experiment.

## Recovered linked chains to integrate

- **ECVR** — culture-aware representation/normalization with chronology, script, phonology, semantics, numeric systems, and uncertainty.
- **RNCE / NOTC** — reversible carrier/operator/transport work; identity paths; radix/nexus generation; reconstructibility and return tests.
- **ARGO NAVIS / OLF / HPOF** — carrier, traversal state, admissibility, return, route/orientation/jurisdiction, and bounded mechanism-first interpretation.
- **RSHE** — recursive symbolic-harmonic operators and cross-representation routing.
- **Human Thought Perspectives Sieve / Council** — explicit parser modules, ablations, adversarial perspective checks.
- **AutoProof Forge / MTCC** — definition lock, assumptions, lemmas, counterexamples, tests, proof skeleton, grading, export.
- **GHAT-OIP / IAST** — operator graph, provenance, implied-assumption tracking, missing-bridge detection.
- **Invariant Ecology Registry** — standing meta-index for all new constructs.
- **Reuptake / Relay** — append-only state transfer and cross-chat/project continuity.
- **Builder / public surface** — website, simulations, documents, apps, and other outputs remain downstream of validated state.

## Reconstructed integration

The previous systems are now treated as one relay rather than competing master frameworks:

```text
Source/Witness
→ Confluence
→ Relay Event
→ IER normalization
→ Nexus bridge proposal
→ Council transforms
→ Adversarial tests
→ Organon experiment choice
→ Builder
→ ResultPacket
→ Relay Event
→ promote / repair / quarantine / supersede
```

This is a reconstruction from preserved architecture fragments, not a claim that this exact line appeared verbatim before.

## Missing and deliberately not invented

The prior conversation referenced an original enumeration summarized as **“12 proofs / 7 domains / 20+ fusions.”** The exact enumeration is not preserved in the accessible state.

A statement that “twelve synthesis routes are recoverable” survived, but the original numbered list did not. Therefore:

- the original list is **Missing**,
- no generated route registry may be presented as the original list,
- any new twelve-route integration must be marked **Generated** until independently matched to source material.

## Generated at this checkpoint

1. GitHub becomes an authoritative implementation surface for Codex state.
2. `hyroglyphx/Codex` is used as the canonical research-state repository.
3. Website artifacts remain separate from kernel/state architecture.
4. A JSON schema is added for OrganonEvent.
5. Δ-001 becomes the first repository benchmark for cross-chat/state reconciliation.
6. A generated route registry maps major living chains into the relay architecture without claiming to restore the missing original list.

## Next development edge

Run Δ-001 against one concrete research lineage with enough history to contain:
- a source object,
- a changed claim,
- at least one contradiction or repair,
- a validated result,
- an unresolved residue,
- and a Builder-facing next action.

The numeral-seven carrier / ECVR lineage is a strong candidate because it already contains positive, partial, and negative controls.
